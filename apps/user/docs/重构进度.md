# BME 前端重构进度追踪

> **最后更新：** 2026-06-11
> **基于计划：** [重构计划.md](重构计划.md)

---

## 进度总览

| 阶段 | 完成数 | 总数 | 状态 |
|------|--------|------|------|
| **P0** 安全修复 | 3 | 3 | ✅ 已完成 |
| **P1** 结构重构 | 1 | 6 | 🔨 进行中 |
| **P2** 架构优化 | 0 | 6 | ⏳ 未开始 |
| **P3** 持续改进 | 1 | 6 | ⏳ 未开始 |
| **新** 本轮新增 | 4 | 4 | ✅ 已完成 |

---

## P0 — 安全与上线前修复 ✅

- [x] **1.1** API 地址改为环境变量 — `api.js` 使用 `VITE_API_BASE_URL`，已有 `.env.development`
- [x] **1.2** Mock 系统环境变量控制 — `config.js` 读取 `VITE_USE_MOCK`
- [x] **1.3** 路由守卫补完 — 17 条路由有 `requiresAuth`，`beforeEach` 统一守卫

---

## P1 — 结构性重构 🔨

- [ ] **2.1** CSS 变量体系 — ⚠️ 仅有 `main.css`（Tailwind 入口），缺变量/主题/全局样式文件
- [ ] **2.2** PageLayout 布局组件 — ❌ `components/layout/` 不存在
- [ ] **2.3** AuthLayout 认证页布局 — ❌ 同上
- [ ] **2.4** 路由懒加载 — ❌ 0/27 动态导入，全部静态加载
- [ ] **2.5** 图标按需引入 — ❌ `main.js` 仍全量注册循环
- [ ] **2.6** 统一滚动条样式 — ❌ 5+ 组件各自定义滚动条

---

## P2 — 架构优化 ⏳

- [ ] **3.1** Store 模块化 — ❌ 仍为单文件 `store.js`（108 行）
- [ ] **3.2** Token 统一来源 — ❌ 3 处独立读取 localStorage
- [ ] **3.3** GroupView 拆分 — ❌ 仍是 1540 行单文件
- [ ] **3.4** Composition API — ❌ `MenuComponent.vue` 仍有双 script 块
- [ ] **3.5** `@/` 路径别名 — ❌ 全项目 0 处使用
- [ ] **3.6** 勋章图片压缩 — ❌ 21 张 PNG 共 38MB

---

## P3 — 持续改进 ⏳

- [ ] **4.1** 拼写修正 — ⚠️ `Attendence/` 旧目录未改名（新的 `Group/` 下已用正确拼写）
- [ ] **4.2** 清理依赖 — ⚠️ `vue-persistedstate` 未使用（`gsap` 仍在用，不删）
- [ ] **4.3** 构建产物分析 — ❌ 未接入
- [ ] **4.4** console.log 清理 — ❌ GroupView 26 处，全项目 128 处
- [x] **4.5** dist 目录清理 — ✅ 无残留
- [ ] **4.6** 基础测试 — ❌ 未接入

---

## 本轮新增 ✅（不在原计划中）

- [x] **N.1** SCSS 移除 — 删除 `_variables.scss`、`_reset.scss`，卸载 `sass`/`sass-embedded`
- [x] **N.2** DesignDemoView 移除 — 文件和路由引用均已删除
- [x] **N.3** Tailwind CSS v4 接入 — 安装配置完成，`preflight(false)` 避免与 Element Plus 冲突
- [x] **N.4** flexible.js 移除 — `main.js` 中已注释，`AboutUsComponent.vue` 的 3 处 rem 已转 px
- [x] **N.5** NotificationView + StudentNotificationComponent 重构 — el 布局 + Tailwind/inline style 内容组件
- [x] **N.6** Flask 后端环境配置 — MySQL/Redis 就绪，seed 脚本编写完成，端口改为 5001

---

## 下一步计划

### 短期（下次会话）
1. **TeacherNotificationComponent** — 同步改为和 Student 版一致的 inline style 风格
2. **Tailwind 组件库** — 在 `src/components/ui/` 下封装基础数据展示控件（Button、Card、Badge、Tag、Tabs、List、Pagination）
3. 确定组件库的设计 token（颜色、间距、圆角、阴影）

### 中期
4. P1 剩余项：路由懒加载、图标按需引入
5. P2：Store 模块化、Token 统一

### 长期
6. 逐步用 ui 组件库替换 Element Plus
7. P3 清理项
