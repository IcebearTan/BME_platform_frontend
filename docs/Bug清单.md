# Bug 清单（2026-09-03 环境测试）

> 来源：API 功能测试 + 前端页面测试。仅列问题，不含测试通过项。

## 后端 Bug

| # | 级别 | 问题 | 位置 | 状态 |
| --- | --- | --- | --- | --- |
| B-1 | 轻微 | 导生名片 capacity 传 0 被静默存为默认 8，应报 400（`d.get("capacity") or 8` falsy 陷阱） | `BME_platform_flask/blueprints/camp_ms.py` profile_put | **已修 09-03**（缺省才补 8，显式 0/null 走范围校验报 400） |

## 需拍板的设计问题

| # | 问题 | 说明 |
| --- | --- | --- |
| D-1 | 批量指派不校验名额 | 单人指派满额 409、有 allow_over；批量指派完全不查 capacity（代码注释称有意）。建议批量结果对超额项给 warning |
| D-2 | 候选人池 min_level=1 会扫进普通学生 | generate-by-level 按等级物化，LV>=1 时全部学生入导生资格池。~~UI 暴露该参数需防呆或下限锁 2~~ **已修 09-03：前后端双锁 LV2-4**（camp.py 400 拒绝 min_level=1 + 管理端选项只剩 LV2/3/4） |
| D-3 | 感谢信可写两封 | 唯一约束按 (sender, recipient, camp_session_id)，无营语境(NULL)与营内各一条，同一导生可收到同一学生两封信 |
| D-4 | 导生无法自己勾选学生 | **已修 09-03**：志愿截止后（done 协调期）导生工作台新增「人员确认」卡——全营名单（志愿序/留言/归属状态/搜索）+ 名额内自助勾选、可释放自己勾选的学员（老师指派行不可释放）。后端 `GET /camp/ms/<sid>/pick/roster` + `POST /camp/ms/<sid>/pick`（行锁防并发、满额 409、单归属冲突 409、账本 source=mentor_pick），与老师批量指派共用写入路径。注：设计方案裁决⑤（09-02）已明确主路径仍是老师导出 CSV → 线下协调 → 批量指派回填，导生自助勾选是并行通道，R-013「导生自勾」表述系过度推断已被纠正 |
| D-5 | 线上三志愿去留 | R-013 字面意思平台不再收集志愿，但市集/志愿功能已开发完成，删留需拍板。**设计方案裁决⑤已拍：保留单轮 3 志愿收集 + CSV 导出 + 线下协调 + 批量指派回填** |

## 前端 UI 问题

| # | 页面 | 问题 | 状态 |
| --- | --- | --- | --- |
| U-1 | 营期中心 | 「报名」与「进入营期工作台」同时显示可点，已入营成员是否应隐藏报名入口 | 待确认 |
| U-2 | 营期中心 | 结束日期临近（明天）仍显示「开放报名」；「进行中+开放报名」徽标并存是否预期 | 待确认 |
| U-3 | 营期中心/工作台 | 状态徽标/阶段过多（营期状态+报名状态+选导生阶段三层叠放），导生看不出哪个阶段能编辑名片；「选导生」tab 仅营内导生可见，入口太深 | 已确认待修 |
| U-4 | 导生市集 (/camp/2/market) | 两行说明文案冗余（截图 03），建议删除或精简 | 已确认待修 |

## 未测项（不可逆，待拍板）

- close 结营归档后的全端点只读保护，需在专用测试营上执行后验证。
