# 营期升级重构 · 设计方案

> 版本：v1.3（2026-09-12 第三次修订：项目营阶段3/4 开工前修订——「学习+交付」三层解耦：ProjectTemplate 实体/CampMilestone 泛化/CampPolicy 能力开关/工作台按 category 组装/H-005 定稿/节点级提交主体/申报窗口收紧，见 §0 09-12 行与 §3.7/§3.8/§3.10；v1.2 志愿流程与裁决全文继续有效）
> 输入：`docs/营期设计需求.md`（老师纪要 + A1–A14 问答）、`docs/功能设计与实施记录.md`（同事，v0.8，含 Q-001~Q-008 最新决策）、代码摸底报告（2026-09-01）
> 范围声明：**本期只做学习型营期（培训营）与项目型营期**（同事文档 R-001）；临床调研型/比赛型不展开。
> 
> **进度快照（2026-09-03 收尾）**：1a/阶段1/阶段2 全部完成并真库联调通过；用户验收通过（09-03）。本批：前端身份漂移修复（user 端两级+营内 my_role）、营期中心（/camp 五分组卡片，状态只在身份行+统一「进入营期」按钮，中心纯导航）、CampJoin 工作台内报名（意向大组+到岗日+理由，项目营按 category 分发预留）、申请撤回（cancel 端点+三处入口）、CampPolicy 落表、admin 端死角色串清扫、CampHome 旧报名 sheet 退役（报名全站唯一入口=工作台 CampJoin）、mock/config.js 死文件删除。e2e 27 绿。**已知留待后续**：市集名片 capacity 显示与 R-009 口径出入（阶段3 roster API 统一时处理）；formation 锁定/调剂工具与 CampUnit/roster 统一 API 并入阶段3。下一步：push → 阶段 3 项目营。
> **进度快照（2026-09-12 v1.3 修订批）**：设计方案修订至 v1.3（三层解耦/H-005 定稿=无锁定环节/节点级提交主体/申报仅 upcoming/capability 开关），阶段3 按本版开工——批次A=组织与申报组队（migrate_20），批次B=模板交付与档案（migrate_21），批次C=e2e+真库联调收口。
> 读者：开发人员（含 AI 辅助开发时作为权威上下文）。与代码冲突时以本文为准；本文与同事文档冲突处见 §10 裁决。

---

## 0. 决策速览（时间序，后者覆盖前者）

| 日期 | 来源 | 决策 | 落点 |
|---|---|---|---|
| 09-01 | A1–A5 | 教学周期实体（无起止日期）；类型封闭枚举；身份解耦两级 super_admin/user；一次性大版本迁移 | §3.1–3.3、§4 |
| 09-01 | 补充决策 | **user_mode 约 48 处门禁一次性清账删列**；Phase 1a（身份解耦）可先行独立部署 | §4.3、§7.1 |
| 09-01 | A6/A7 | 营内角色=报名审批/后台拉人；任职记录从简+预留评价 | §3.5 |
| 09-01 | A8–A13 | 单轮 3 志愿 + CSV 导出 + 批量指代回填；capacity 内部化；大组=类别标签；课程独立、进度走营期；移除自主加入学习 | §3.9、§3.12 |
| **09-02** | Q-004 | 志愿流程简化：**保留**平台单轮收集 3 志愿 → 导出 → 线下协调 → 回填正式归属；**废除**两轮在线反选、抢选、自动匹配、表格导入（注：同事文档 R-013「平台不收集志愿」为其会话的结构化过度推断，用户 09-02 已口头纠正，见 §10 裁决⑤） | §3.9 |
| **09-02** | Q-001/Q-006 | 项目营：每人同营期最多参与 **3 个项目**（自己负责的计入，负责人还可加入 2 个）；一人最多负责 1 个项目，无联合负责人；负责人由老师线下指定→平台申报→管理员审核生效 | §3.8 |
| **09-02** | Q-002/R-003 | 生命周期归**具体营期**（周期只归类统计、无状态、无草稿）；同周期各营期状态独立 | §3.3 |
| **09-02** | Q-003 | 结营=**关键事实冻结 + 受控修正版本化留痕**；不建独立资料库，只读聚合 | §3.11 |
| **09-02** | Q-005 | 项目营第一期=申报版本/审核/公开展示/负责人勾选团队/**中期结题节点/材料版本/基础成果核验**；课程、任务、经费后置 | §8 阶段 3–4 |
| **09-02** | Q-007 | 学习营导生=**管理员导入资格名单→确认→名单内定向可见→自行报名（无二次审核）→布置名片**；非名单用户看不到导生入口 | §3.6 |
| **09-02** | Q-008 | 课程策略：学习营 admin_managed（管理员负责+导生共建授权）、项目营 unit_creator（负责人范围内开课）；CourseOffering/Collaborator/Enrollment 三表（后置阶段 5） | §3.12 |
| **09-02 补①** | 候选人池策略化 | 营期外链「导生候选人池」（CampMentorEligibility+source），进池策略可插拔：手工导入（Q-007 保留）/ 按等级生成（user.level LV1-4 物化进池）；报名门禁只查池，与等级解耦；手工可覆盖自动 | §3.6 |
| **09-02 补②** | 导生报名审核制 | 池内资格=可报名；报名创建 apply_role=mentor 的加入申请，**管理员审批后入营**（修订 Q-007"无二次审核"） | §3.6 |
| **09-02 补③** | 状态机冻结 | 用户拍板五态 draft/upcoming/selecting/running/archived（选择阶段独立），无 cancelled，结营不可逆；子窗口方案作废 | §3.3 |
| **09-02 补④** | 报名进工作台+选组 | 学员报名在营期工作台内进行（非中心弹窗）：选意向大组（本营 ms_tags，有标签时必选）+ 承诺到岗日 + 理由，`camp_join_request + preferred_tag`（服务端校验须在 ms_tags 内）；**项目营报名机制可能不同，前端按 category 分发预留（阶段 3）** | §3.5、§6 |
| **09-02** | 状态基线（H-004 未冻结） | 主状态 `preparing/running/closed/cancelled` + 子流程 `application_phase`（报名窗口）+ `formation_phase`（勾选窗口） | §3.3 |
| **09-11** | D-1/D-3 拍板 | 名片名额**缺省=不限**（capacity NULL，废除 09-03 缺省补 8；显式 1-30 校验、0 仍 400，不限时不设满额门槛）；批量回填**维持不校验名额**；感谢信无营路径维持现状不收紧 | §3.6、camp_ms.py |
| **09-12** | 导生自由报名（取代 Q-007/补①/补②的名单机制） | 资格名单（CampMentorEligibility）**退役**；LV≥2 在 upcoming/selecting 窗口自助报名（审核制保留）；admin 导入语义改为**直接导入为导生**（members/batch role=mentor） | §3.6 |
| **09-12** | 功能扩展轮立项 | 项目营「学习+交付」**三层解耦**：模板管共性/里程碑管交付/课程管学习（不整体抽象成课程，防伪课程污染目录与搜索）；项目营=营期 **category 扩展**（域归属判据：组织生命周期同构→扩展，流程骨架异构→独立域，赛事营走独立域另案）；考勤/请假/座位等=CampPolicy **能力开关**（learning 默认开、project 首期关，营期行可覆盖）；阶段3 原定「项目广场」公开展示并入全站「项目展示平台」新板块（另案），营内只做组队浏览 | §3.3、§3.7、§3.10、§6 |
| **09-12** | H-005 定稿 | 项目营**不设 formation 锁定环节**：勾选窗口=营期状态（selecting 自由勾选，running 起关闭），变更一律走管理员通道（原因必填+事件+行状态化 ended）；学习营维持现状不建锁定工具 | §3.8 |
| **09-12** | 里程碑提交主体 | **节点级配置** `submit_mode`：team（整队交/负责人提交/老师审）或 member（成员个人交/负责人审）；「负责人可提交不可自审」红线保持 | §3.10 |
| **09-12** | 项目申报窗口 | **仅 upcoming**（申报期）；selecting 起申报入口关闭，无回拨迁移 | §3.9 |
| **09-12** | 砍学员报名意向大组 | 报名不再选组（与导生名片 tags 双选组冲突）；**组别随归属导生继承**（导生组=名片 tags）；`preferred_tag` 列保留存历史行，两端回读与 admin「意向组」列移除 | §3.5 |
| **09-12** | 志愿数放宽 | 学员志愿**1-3 个即可提交**（原单轮必须满 3；上限 3 与有序性保留，后端本就是 1≤n≤3，前端托盘解锁） | §3.9 |
| **09-12** | 开营即截止选导生 | `open` 迁移（selecting→running）联动：志愿截止仍在未来则压到当前时刻（一步停掉选导生进正式开营；演示/实战杠杆），清 Redis 阶段游标让 done 通知可发；未分配学员归属可开营后再指定 | §3.3 |
| **09-12** | 方向制学习（砍学生选课） | 学生自主选课整体下线（/camp/selection 两端点删除）；入课唯一途径=**归属导生继承**；learning 营课程目录由方向派生（CampCourse/admin 课程 tab 收窄到项目营） | §3.5、§3.6 |
| **09-12** | 分类=方向+课程 | ms_tags JSON 升级对象数组 `[{name, course_id}]`（零迁移，legacy 字符串容忍读名、编辑强制补课程）；`_validate_ms`：enabled 时每方向必须绑存在课程；**导生名片单方向**（tags 恰 1） | §3.6 |
| **09-12** | 导生按章认证学习进度 | 新表 camp_chapter_certification（migrate_24，UQ 营+学员+章节）；导生在团队成员卡逐章认证/撤销（学员自报课时完成比作参考）；**全章认证齐→user_course.status 自动 completed**（死常量启用） | §3.6 |
| **09-12** | 建营弹窗只收基本信息（draft 有意义） | 选导生开关/志愿时间/方向课程绑定**全部迁到营期详情「选导生」tab 配置卡**（learning 营恒显；draft/upcoming 可编辑，开跑后锁定只读并展示方向+课程一览）；ms tab 不再要求已启用才可见 | §3.6 |
| **09-12** | admin 详情 tab 按生命周期重组 | 「加入申请」改**学员申请**（纯学员，导生报名挪走）；「导入导生」tab 删除——导生全生命周期并入**「选导生」一页三段**：招募（待审导生报名+导入即导生）→ 方向与配置 → 流程运营 | §3.6 |

---

## 1. 目标与非目标

### 1.1 目标

1. **身份解耦（Phase 1a，先行）**：全局角色收敛两级 + 约 48 处 `user_mode` 门禁清账删列 + `camp_role` 改查营内任职。与营期细节完全正交，可独立部署。
2. **共用营期骨架**：CampCycle 周期归类 + category（learning/project）+ 独立生命周期（主状态+双子流程）+ CampPolicy 配置 + 统一营期中心。
3. **单轮志愿 + 线下协调 + 回填确认**：导生资格名单定向报名；学员入参与者池并在市集页**提交 3 志愿（单轮）**；老师**导出志愿 CSV → 线下与导生协调 → 批量指派回填**（导生可经勾选页自助确认，共用 roster API）；服务端做唯一性/上限/权限校验，沉淀正式归属 + 只追加事件。
4. **项目营闭环**：申报版本→审核→勾选组队（3 上限）→模板化里程碑节点→材料版本→成果核验→结营档案（公开展示并入项目展示平台板块，v1.3 另案）。
5. **结营档案**：关键事实冻结、普通写全关、档案修正版本化留痕。
6. **课程独立、进度绑营**：移除自主加入学习；课程委托/范围授权后置阶段 5。
7. **留痕**：CampMembershipEvent + CampAuditEvent 只追加，作为未来等级评价系统的数据底座。

### 1.2 非目标（本期不做）

- 两轮在线候选池、抢选、自动匹配引擎、线下表格导入平台（B-004）
- 临床调研型/比赛型营期（R-001）
- 独立资料库/通用文件夹（R-005）；项目任务、经费台账、复杂多评委评分（Q-005）
- 等级评价系统、干事体系、搜索、个人主页、感谢信模板、站内信（Phase 3+）
- `check_record.camp_session_id` 落实写入（已知债不动）

---

## 2. 总体模型

```
平台身份（两级）：super_admin / user            ← Phase 1a 收敛，§4
CampCycle 营期周期（2026暑等，只归类统计，无状态）
└── CampSession 具体营期（learning | project，独立生命周期+双子流程+CampPolicy）
    ├── CampParticipant 参与者池（营期身份；由 camp_member 演进）
    ├── CampMentorEligibility 导生资格批次（学习营；名单→确认→定向可见）   ← 学习营
    ├── CampUnit 组织单元（mentor_team | project）
    │     ├── mentor_team：导生报名确认后自动建，1 导生 1 组               ← 学习营
    │     └── project + ProjectProfile + ProjectApplicationVersion         ← 项目营
    │           └── ProjectTemplate（节点施工图）→ CampMilestone + CampSubmissionVersion（v1.3 三层解耦交付层）
    ├── CampUnitMember 单元成员（单元身份：leader/member；项目 3 上限）
    ├── CampLearningMatch 学习营正式归属（每生一导生，强唯一）              ← 学习营
    ├── CampMembershipEvent / CampAuditEvent（只追加留痕）
    ├── CourseOffering → Course（阶段 5，范围内开课）
    └── CampArchive / CampArchiveRevision（结营冻结+受控修正）
```

三层身份映射（融合同事 §11.4 与两级平台角色，见 §10 裁决①）：

| 层级 | 取值 | 载体 |
|---|---|---|
| 平台身份 | super_admin / user（+admin_tag 审计标签） | `user.role` |
| 营期身份 | organizer / manager / participant 等 | `CampParticipant.role` |
| 单元身份 | mentor_leader / project_leader / project_member | `CampUnitMember.role` |

---

## 3. 数据模型

约定：无 Alembic，`scripts/migrate/` 幂等脚本（§7）；沿用同事 §14 的实体命名，与现状表的映射如下。

### 3.1 User：全局角色收敛（不变，Phase 1a）

`role ∈ {super_admin, user}`（teacher→super_admin，mentor/student→user）；`+ admin_tag(teacher|developer)` 仅审计；`user_mode` **清账删列**（§4.3）；新增 `PUT /admin/users/<id>/role`（任命管理员唯一入口）。

### 3.2 CampCycle（原方案 term 改名对齐）

`code('2026-summer') UNIQUE / name / sort_order`。无起止日期、无状态、无草稿（R-003、A1）。建营时下拉选择+行内新建。

### 3.3 CampSession：类型与生命周期

| 变更 | 内容 |
|---|---|
| `+ category` | `learning | project`（学习型=培训营；research/competition 枚举预留不实现）。存量全部回填 `learning`；旧 `camp_type` 退役 |
| `+ cycle_id` | FK CampCycle，新建必填 |
| **状态机定稿（H-004，用户 2026-09-02 拍板，取代同事文档基线）** | 五态线性：`draft` 草稿(筹备，仅管理员可见) → `upcoming` 待开放(导生报名+名片布置+admin 可导入导生；学员见卡片无报名入口) → `selecting` 选择阶段(学员报名入池+导生报名窗口仍开+市集交志愿+导出/指派/锁定团队) → `running` 进行中(课程/考勤/请假/奖励) → `archived` 已结营(只读+档案)。**无 cancelled**（黄掉的营：draft 直接删，开营后黄的直接结营）。迁移 draft→draft、active→running、archived→archived；迁移走 `POST /transitions`（目标动作制，条件更新+审计）；upcoming→draft 允许撤回，结营不可逆。原 application/formation 子窗口方案作废——窗口即状态。09-12 注：upcoming 描述的「导生资格导入」随名单机制退役，导生入营=自助报名（LV≥2）或 admin 导入 |
| `+ policy` | FK CampPolicy：报名方式、勾选方式、项目参与上限（默认 3）、课程策略、**能力开关 capabilities（v1.3：attendance/leave/seat 等 JSON 位——learning 默认全开、project 首期全关，营期行可覆盖；后端按位门禁端点，前端按位渲染 tab，考勤/请假/座位等能力不定死于 category）**；类型默认值写在代码常量，营期行可覆盖（避免主表膨胀） |
| `ms_*` 字段 | `mentor_selection_enabled/ms_tags/ms_preference_start/ms_preference_deadline` **保留**（名片标签 + 单轮志愿窗口）；`ms_round1_deadline/ms_round2_deadline` 退役（无第二轮） |
| 可见性 | 营期中心列表：running/可报名 preparing 对全员；历史（closed）对参与者；**preparing 的导生入口仅资格名单内可见**（§3.6），营期卡片本身是否对全员展示按营期中心规则（待我处理/即将开始分组），非名单用户不渲染导生入口 |

### 3.4 CampParticipant（camp_member 演进，保留表名避免外键 churn）

营期人员池 + 营期身份。现有 `camp_member` 即此表：`UQ(camp,user)` 保留；`role` 语义升级为营期身份（`participant/mentor/organizer…`，现状 student/mentor 值兼容读取）；`team_mentor_id` 退役路径=由 CampLearningMatch 承接（迁移期双写，读切流后删列）。`source/appointed_by/joined_at` 保留；评价字段预留（A7）不变。

### 3.5 CampJoinRequest（入池申请）

保留现有表与审批流，`apply_role` 语义改为营期身份申请（学员入池）。管理员可直接拉人入池（source='assign'）。**09-02 补④**：+`preferred_tag`（报名时选的意向大组，服务端校验须在本营 ms_tags 内；admin 申请列表与 mine 均带出）——**09-12 砍掉**：学员报名不再选组（与导生名片 tags 双选组冲突），组别随归属导生继承；列保留存历史行，新申请不写、两端回读与 admin「意向组」列移除；报名入口在营期工作台内（CampJoin，按 category 分发，项目营机制阶段 3 预留）。**09-03**：+`POST /sessions/<sid>/join-request/cancel` 撤回本人 pending 申请（导生报名/学员入营通用，手滑可反悔；撤回后可重提新行，已审批历史不可撤）；前端三处撤回入口（工作台导生报名卡/CampJoin 待审核卡/CampHome 招募页）。

### 3.6 导生招募（**09-12 定稿：自由报名 + 导入即导生，资格名单机制退役**）

> 2026-09-12 用户拍板：与「自由开放报名」运营方式冲突，CampMentorEligibility/Batch 两表**退役**（表保留不删免迁移，蓝图引用已清），替代机制如下。

- **自助报名（主路径）**：learning 营、非成员、非超管、**LV≥2**（D-2：LV1=普通学员）在报名窗口 **upcoming + selecting** 内可自选导生身份报名（`POST /sessions/<sid>/mentor-registration`，门禁顺序=404→400 超管→400 项目营→403 LV<2→200 已成员→400 窗口外→pending 去重→落库 `apply_role='mentor'`）→ **管理员审核**（加入申请 tab 批准，approve 即 CampMember(role='mentor')）。selecting 期用户端导生报名卡与学员报名表单并存；导生 pending 与学员 pending 互斥（后端按 (camp,user) 去重不分角色）。
- **导入即导生（管理通道）**：admin 营期详情「导入导生」tab（learning+超管）——粘邮箱或「按等级填充」（`POST /sessions/<sid>/mentor-import/candidates-by-level`，只读，LV≥min_level、排除超管与在营成员）→ 预览（`/mentor-import/preview`，already_member 标注）→ 确认导入走 **`POST /sessions/<sid>/members/batch` items=[{user_id, role:'mentor'}]**（复用批量加成员事务端点，逐项回报）——**不经报名审核直接入营**。成员管理加成员弹窗指定角色=导生同通道。
- 报名后可布置导生名片（沿用 CampMentorProfile：照片/bio/tags；**capacity 退役**——R-009 无统一容量，管理端只显示人数统计）。
- 旧资格端点（mentor-eligibility 三件套 + generate-by-level 写库版 + candidate-remove）已下线（404）；session_list 不再有 `has_eligibility` 字段，可见性 OR 条件去掉资格分支。

### 3.7 CampUnit + 项目实体（新表）

```
CampUnit:   camp_session_id, unit_type(mentor_team|project), name, status('active'|'paused'|'terminated'), visibility, owner_user_id
            UQ(camp, unit_type, name)
ProjectProfile:       unit_id UNIQUE FK, 背景/目标/所需能力/招募说明/计划/公开范围(visibility='camp'|'public'，public=可发布到项目展示平台)
ProjectApplicationVersion: camp_session_id FK, unit_id(可空——过审时建单元), version, submitted_by, leader_user_id,
                           快照字段(名称/背景/目标/所需能力/招募说明/计划), status('draft'|'pending'|'approved'|'rejected'),
                           reject_reason, reviewed_by/at
```

- mentor_team：导生报名确认时自动创建（1 导生 1 组，name=导生名），无独立管理界面。
- project：负责人申报（版本化，退回重提不覆盖）→ 管理员审核通过时负责人关系生效（Q-006：老师线下指定，平台不遴选；同营期一人最多负责 1 个，无联合负责人）→ 通过后进营内项目列表（组队浏览）。
- **大组（软件组/硬件组）不建表**：沿用 `ms_tags` 标签体系（导生名片挂标签，筛选按标签）——A13-1「仅类别标签无管理」的最小实现；若后续需结构化再升 CampUnit kind=direction（见 §10 裁决③）。

**（v1.3 扩）项目营组队三件套与生命周期映射**：

- 组队志愿表：`CampProjectPreference`（camp, student_user_id, unit_id, rank 1-3, note；UQ(camp,student,rank) 与 UQ(camp,student,unit)）——与导生志愿（CampMentorPreference）同构但 FK 主体不同，**不共表**（防多态外键）。
- 生命周期映射（与学习营共用五态，category 语义差异；这正是「组织生命周期同构→category 扩展」判据的兑现）：
  - `upcoming` = **申报期**：负责人提交申报（版本化）；非成员工作台视图=申报入口卡。
  - `selecting` = **报名+组队期**：学员报名入池（CampJoin project 分发）→ 项目志愿（单轮 3 意向）→ 导出 CSV → 线下协调 → 负责人勾选/老师批量回填（3 上限校验）。
  - `running` = **交付期**：普通勾选关闭，成员变更走管理员通道；模板实例化/里程碑/提交/成果。
  - `archived` = 档案。
- 申报窗口**仅 upcoming**（09-12 拍板），selecting 起关闭；申报时负责人无需预先入池——**过审即自动入池**（CampMember role='member', source='application'）。
- 一人一营最多负责 1 个项目（active 申报 + active 单元 leader 双重校验）；项目营营期成员 role 用新值 `member`（通用入池值，负责人身份在单元层 CampUnitMember.role='leader'），学习营 student/mentor 值不变。
- 营期工作台「项目」tab 承担组队浏览/志愿/我的项目（`已参与 n/3`）；**公开「项目广场/详情公开区」移出本域**——并入全站「项目展示平台」板块（功能扩展轮另案：发布=负责人/admin 显式动作，visibility 为权限基础，不反向驱动营期流程）。

### 3.8 CampUnitMember + CampMembershipEvent（单元身份与留痕）

```
CampUnitMember: unit_id, user_id, role(leader|member), status(active|ended), started_at, ended_at
                UQ(unit, user)
CampMembershipEvent: camp_session_id, unit_id, user_id, action(select/deselect/adjust/exit/remove),
                     source(offline_confirmation|admin_adjust), operator_id, before/after, occurred_at   （只追加）
```

- **统一勾选 API**（学习/项目共用）：`GET /camp/units/{unitId}/selection-roster`（候选名单：资格、已选状态、`current_project_count/remaining_project_slots`、当前各单元身份）+ `PUT /camp/units/{unitId}/member-selection`（`expected_version` + `idempotency_key` + operations[]，逐项返回 added/removed/unchanged/conflict/forbidden）。v1.3 注：roster API 建成 **unit 通用型**；学习营现役 `/pick` 与 `assign/batch` 首期不迁移（防回归），迁移窗口=下个学习营开营前。
- 学习营规则：每生一导生——`CampLearningMatch(camp, student_user_id UNIQUE, mentor_user_id, unit_id)`（由 camp_mentor_match 演进），并发勾选锁参与者行，冲突返回现有归属（`LEARNING_ASSIGNMENT_CONFLICT`）。
- 项目营规则：每人同营期计入上限的有效关系 ≤3（CampPolicy.project_limit，默认 3，负责人自己的计入）；事务内锁 `(camp,user)` 参与者行重计数后写入（`PROJECT_MEMBERSHIP_LIMIT_REACHED`）；批量按 user_id 排序加锁防死锁，逐人独立成败。
- **H-005 定稿（09-12，用户）：不设 formation 锁定环节**——项目营勾选窗口即营期状态：`selecting` 期负责人自由勾选/老师批量回填，开营（selecting→running）后普通勾选关闭（返回 `CAMP_FORMATION_CLOSED`），此后一切成员变更（退出/移除/换组/补人/负责人变更/项目暂停终止）一律走**管理员通道**：原因必填 + CampMembershipEvent 留痕 + 行状态化 ended（历史提交与贡献保留、3 上限名额即时释放）。学习营现状维持（志愿截止=协调期，开营后变更走成员管理），不建锁定/解锁工具。

### 3.9 选人总流程（v1.2 修正：平台收集志愿，线下协调，回填沉淀）

```
[学习营] 资格名单导入确认 → 名单内导生定向可见并自行报名 → 建组+布置名片
        → 学员申请入池（application 窗口）→ 志愿窗口内市集页提交 3 志愿（单轮，仅选导生，组别继承）
        → 老师导出志愿 CSV → 线下与导生协调/调剂 → 批量指派回填（主路径）
          （导生也可在勾选页自助确认线下已定学员，共用 roster API）
        → 服务端单归属校验 + CampLearningMatch 沉淀 → formation 锁定 → running：课程/考勤/请假/奖励 → closed 档案
[项目营] 老师线下指定负责人 → 平台申报（版本化，仅 upcoming 申报期）→ 管理员审核（负责人关系生效+自动入池）→ 营内项目列表
        → 参与者入池（selecting）→ 项目志愿（单轮，同构，学员可填 3 个项目意向）
        → 导出/线下协调 → 负责人勾选或老师批量回填（3 上限校验）→ 开营 running（普通勾选关闭，变更走管理员通道）
        → 模板实例化/节点/材料版本/成果 → 结营档案
```

- **CampMentorPreference 继续使用**：`round` 恒为 1（UQ 兼容现状）；志愿窗口用 `ms_preference_start/deadline` 控制。
- **删除**：round1/round2 两轮机制、`/pick` 在线收人、自动匹配/结算逻辑；`CampMentorMatch` 演进为 CampLearningMatch（provenance 保留）。
- **新增**：`GET /camp/ms/<sid>/export`（志愿宽表 CSV：学员/志愿 1-3/留言/当前归属）+ `POST /camp/ms/<sid>/assign/batch`（幂等批量回填，逐项成败）。
- **市集页保留**为单轮志愿收集入口（名片墙 + 志愿托盘；阶段条简化 collecting→done），沿用刚合并的视觉优化成果；项目营「项目广场」复用名片墙/筛选组件。

### 3.10 项目模板与里程碑交付（v1.3 泛化：两档 → 模板化多节点；「学习+交付」三层解耦的交付层）

**三层解耦（09-12 拍板）**：课程教方法，模板定关卡，里程碑是关卡的实际发生。依赖单向——里程碑←模板，模板可引用课程（软链不复制），课程不感知另外两者。红线：① 课程里不放「要交的东西」，交付物一律走里程碑；② 里程碑节点可**引用**课程/资源作学习材料（软链不复制）；③ scope=project 的 offering 不进公共课程目录与搜索默认结果（阶段5 消费）；④ 模板推荐课程=引用而非复制。

**ProjectTemplate（项目模板=节点施工图，管共性）**：

```
ProjectTemplate:      id, name, description, scope('platform'|'unit'), category(适用项目类别,可空),
                      camp_session_id(空=平台模板), unit_id(UQ,unit 模板必填), created_by,
                      cloned_from_id(自引用复制链), status('draft'|'active'|'archived'), created_at
ProjectTemplateNode:  id, template_id, sort_order, title, description(节点说明),
                      deliverable_req(交付要求), material_note(材料模板说明),
                      recommended_course_ids(JSON 软链), submit_mode('team'|'member')
```

- 创建者=**项目负责人**（09-12 拍板）；三种起点：空白自建 / 平台默认模板（scope='platform'，admin 维护页按 category 筛选，首期不做推荐权重）/ 复制历史项目模板（cloned_from 溯源链；结题归档后的模板可复制=资产回流入口）。
- 实例化：过审项目由负责人选起点建 unit 模板 → 节点序列生成 CampMilestone；实例化后可增删节点、调时间——「大体相同但有差异」的正确抽象。

**CampMilestone（里程碑=关卡的实际发生，管交付）**：

```
CampMilestone:  id, camp_session_id, unit_id, node_id(来源节点,可空——删节点不级联删里程碑),
                title, description, requirement, due_date, order_no,
                submit_mode('team'|'member',实例化随节点落入), status('open'|'submitted'|'returned'|'approved')
```

**CampSubmissionVersion（版本化提交，双模式）+ 附件**：

```
CampSubmissionVersion:  id, milestone_id, version, submitted_by, content,
                        status('submitted'|'returned'|'approved'|'superseded'),
                        reviewed_by, reviewed_at, review_note, created_at    UQ(milestone,version)
                        （member 模式每人一条版本链，加 submitted_by 进 UQ 语义；附件=MinIO 子表，见下）
```

- 提交主体**节点级配置**（09-12 拍板）：`submit_mode='team'`（整队交）=负责人提交、老师（admin）审，每个里程碑一条版本链；`submit_mode='member'`（个人交）=成员各自提交、负责人审（负责人自己份额交老师审，「负责人可提交不可自审」红线保持），每人一条版本链，里程碑状态聚合（全员 approved → approved）。
- 退回重提=新版本（旧版 superseded）；审核绑定期望版本（乐观锁，冲突报 `SELECTION_VERSION_CONFLICT` 语义）。
- 附件走 MinIO（course resources 同款 multipart 上传+代理下载，storage.py），交付物不落本地磁盘。

**CampOutcome（成果核验）**：`unit_id, title, description, 附件引用, contributor_ids, status('draft'|'submitted'|'verified'|'rejected'), verified_by/verified_at`——**未核验不入个人档案**（纪律沿用）。

**资产回流（09-12 补拍板）**：结题/结营冻结时——① 该项目模板 status='archived'（可被复制起步）；② 验收通过的交付附件打标留存为**数字资产**。数据本轮就位（随 CampArchive 冻结+打标），浏览落点=项目展示平台板块（另案）；深度资产库/跨项目检索归下一阶段内容沉淀。

### 3.11 CampArchive（结营档案，阶段 4；阶段 1 只补写保护）

`POST /archive` 幂等结营：冻结成员与角色、学习归属、项目与角色、出勤请假汇总、课程完成摘要、奖励、成果版本、**里程碑与验收交付（v1.3：随档案触发模板 archived+资产打标）**；生成 `CampArchive`。此后所有普通写统一拒绝（**现状 `_camp_writable` 有遗漏，阶段 1 修复**），修正走 `POST /archive/revisions`（专门权限+原因+期望版本→新版本递增，`CampArchiveRevision`）。旧 archived 营期不补建档案行（结营冻结只对增量生效，v1.3 裁定；历史营期的只读聚合展示走查询态）。跨年续做项目=新营期内**重新申报**（沿用 ProjectProfile 复制，且可复制历史模板起步），不自动继承（简化，替代昨天「按归档状态重启」的说法，见 §10 裁决④）。

### 3.12 课程（阶段 5；阶段 1 只做减法）

- 阶段 1：移除 `/userCourse/enroll` + 前端「加入学习」（A14），课程独立实体独立页面维护，学习入口只走营期。
- 阶段 5：`CourseOffering`（范围=全营/导生组/项目）+ `CourseCollaborator`（管理员授导生共建）+ `CourseEnrollment`（加入实例，UQ(offering,user)）；学习进度关联 enrollment——**这就是 Q15 的正式答案**（原方案 B 的落地形态），届时 LearningProgress 按 offering 实例分档；现有 CampCourse 迁营期级 offering。
- 现有 `GroupModel/CourseGroup` 僵尸债务不动。

---

## 4. RBAC 与权限（Phase 1a 不变，营期侧按新模型）

- §4.1/4.2/4.3 全部维持昨日内容：两级角色+admin_tag；`camp_role` 重写为营内任职检查；`is_admin()` 唯一收口；约 48 处 `user_mode` 门禁清账（恒等替换+preflight 校验+零引用验收+9 蓝图冒烟矩阵）；双写红利不再依赖（列直接删除）。
- 新增：权限判断在三层展开——平台（super_admin 直通）→ 营期（CampParticipant 身份+阶段）→ 单元（CampUnitMember 角色）。后端权限矩阵见同事 §11.5；前端按钮显隐不替代后端鉴权。

---

## 5. API 契约要点

沿用同事 §15 全部端点与稳定错误码（`CAMP_NOT_VISIBLE / CAMP_PARTICIPANT_REQUIRED / CAMP_FORMATION_CLOSED / CAMP_ARCHIVED_READ_ONLY / UNIT_MANAGEMENT_FORBIDDEN / LEARNING_ASSIGNMENT_CONFLICT / PROJECT_MEMBERSHIP_LIMIT_REACHED / PROJECT_NOT_APPROVED / SELECTION_VERSION_CONFLICT / IDEMPOTENCY_CONFLICT`）。补充 Phase 1a 自有端点：`GET/POST /camp/cycles`、`PUT /admin/users/<id>/role`。契约红线：列表返回明确数组+分页；写接口检查状态/阶段/角色/版本/幂等键；批量勾选逐项回报，不允许把部分成功显示为全部成功。

---

## 6. 前端要点

- **营期中心**：升级 `/camp` 为统一营期中心（分组：待我处理/我的营期/可报名/即将开始/历史；卡片=周期+类型+状态+截止+本人身份+主操作），`/camp-home` 收敛为着陆页。
- **人员确认页**（两营共用组件骨架）：固定高度工具栏+名单区，搜索/筛选/勾选/差异预览（保存前显示新增与移除人数）/逐项冲突定位；覆盖加载/空/无权限/阶段锁定/失败状态。禁止大而全 `if category` 万能页——公共外壳+类型子视图/service。
- **项目营页面（v1.3 修订）**：营期工作台「项目」tab（selecting=组队浏览+志愿托盘，复用市集视觉模式；running=我的项目 `已参与 n/3`）/项目工作区（负责人：模板+成员勾选+节点）/申报与审核（admin，CampSessionDetail 新 tab）/节点与成果。**组队浏览维持 tab 内嵌（09-12 用户拍板）**——曾议升格为独立出血页（/camp/:sid/pmarket，市集同构），裁定后置；届时工作台 tab 瘦身为状态卡+CTA。公开「项目广场/详情公开区」移入全站项目展示平台板块（功能扩展轮另案），营内不另建公开广场页。工作台 tab 按 **category+单元身份**组装（learning 维持学员/导生两套现状，project=成员/负责人视图；禁 if-category 万能分支组件——公共外壳+类型子视图纪律），考勤/请假等 tab 按 policy capabilities 渲染。
- **保留**：市集页为单轮志愿收集入口（名片墙/志愿托盘沿用视觉优化成果，阶段条简化为 collecting→done）；**退役**：两轮候选池、导生在线收人（pick）、自动结算、表格导入入口。
- e2e：同步调整（营期中心、资格名单可见性、志愿导出、批量指派与勾选冲突、3 上限、归档只读）；**E2E/mock 通过 ≠ 真库验证**（§7.3）。

---

## 7. 迁移与验证

### 7.1 migrate_10_identity（Phase 1a，不变）

等价性校验 → role 刷新（teacher→super_admin；mentor/student→user）→ admin_tag → **DROP user_mode** → rollback 脚本。代码侧：camp_role 重写、48 处门禁清账、payload 删键、role 端点。可独立部署，不需要强制结营，前端不同发。

### 7.2 migrate_11+（营期 schema，按同事 §14.5 分批）

批次顺序：①CampCycle+category/cycle_id/状态字段+CampPolicy（含旧 camp_type 拆分映射与待人工清单）→ ②资格名单表+camp_member 语义升级（role 映射、存量 mentor 回填历史资格）→ ③CampUnit/项目实体/单元成员/事件/学习归属表（camp_mentor_match→CampLearningMatch 适配）→ ④（阶段 4）里程碑/成果/档案 → ⑤（阶段 5）课程三表+UserCourse/LearningProgress 增量迁移。每批 dry-run/执行报告/校验 SQL/回滚边界；不删旧列；双读核对后切流。

### 7.3 验证分层（同事 §18 全文采纳）

后端规则测试（权限/阶段/唯一/上限/错误码）→ **真实 MySQL 事务测试**（并发勾选同一学员、并发抢最后名额、批量部分成功、幂等键）→ 迁移测试（空库/存量/重复执行/回滚）→ 前端组件与营期中心/确认页 → 预览 API 契约同步（5002 fixtures 增加 roster/3 上限数据）→ Playwright e2e → 人工角色检查。**未经真库验证不得标记可交付。**

---

## 8. 分期计划（融合后）

| 阶段 | 内容 | 备注 |
|---|---|---|
| **1a 身份解耦 ✅完成(09-02, flask 5dc125c/17284cf；前端清账 09-02 深夜补齐)** | migrate_10 + camp_role 重写 + 48 处门禁清账 + role 端点 + seed；**后补**：user 端 ~8 处全局角色分支迁两级（staff=super_admin；导生/学员视角改营内 my_role），sessions 回包 + my_role/has_eligibility，e2e fixtures 迁两级 | 与营期细节零耦合；详见 v1.0 §8.1a 工作项 |
| **0 规则冻结（③状态图已冻结✅；①资格标识已由等级策略弱化(email 兜底)；②H-005 待确认）** | ①资格名单导入唯一标识（学号? 邮箱? 未注册用户处理）②H-005 团队锁定后变更细则 ③H-004 状态图最终冻结 | 不阻塞 1a |
| **1 共用骨架 ✅完成(09-02 深夜收尾)** | CampCycle/category/状态机+transitions/归档写保护补漏/移除自主加入学习 均已落地；**CampPolicy 落表（表+建营按类型默认值生成+PUT 覆盖+migrate_15 存量回填，project_limit/course_policy 阶段3/5 消费）；营期中心 UI（/camp 无 sid=中心：待我处理/可报名/我的营期/即将开始/历史 五分组卡片+申请入营弹窗+导生报名；session_list 可见性放宽 upcoming/selecting 对全员）**；/camp-home 收敛为招募着陆页 | 保持现有学习营可用 |
| **2 线下确认设施 ✅完成(09-02, 含真库全链路联调)** | 资格名单→**候选人池策略化(手工/按等级LV)** / 定向可见+报名审核制 / ms 单轮化+志愿导出 CSV+批量指派 / 学习营单归属(行锁) 均已落地并真库联调通过；**09-03 增补：导生自助勾选通道（D-4）——done 协调期导生工作台「人员确认」卡（`/camp/ms/<sid>/pick/roster` + `/pick`，行锁/满额 409/释放仅限 mentor_pick 行，与批量指派共用写入路径），真库硬校验 16 项通过（dev_seed_ms_test.py，gitignored）；候选人池 min_level 前后端锁 LV2-4（D-2），capacity=0 falsy 陷阱修复（B-1，详见 docs/Bug清单.md）**；**未竟：formation 锁定+调剂工具（暂用批量指派+成员管理代替）、CampUnit/roster 统一 API（并入阶段 3）** | 学习营新流程可用 |
| **3 项目营闭环（09-12 开工=v1.3，本轮主战役）** | migrate_20：CampUnit/ProjectProfile/ProjectApplicationVersion/CampUnitMember/CampMembershipEvent/CampProjectPreference 六表 + CampPolicy.capabilities（JSON 位，存量营期回填）/ 申报版本流（仅 upcoming，退回重提新版本）/ 审核（过审建单元+负责人生效+自动入池）/ 项目志愿+CSV 导出+批量回填（逐项回报）/ 统一 roster API（unit 通用型；学习营现役流首期不迁）/ 3 上限行锁校验 / 变更场景（退出/移除/换组/负责人变更/暂停终止——无锁定环节，H-005 定稿）/ capability 门禁（考勤/请假/座位端点按位）/ admin 批量加成员事务化 + gate 残留修复（CampSessionDetail:447/:524）/ CampJoin project 分发 / 工作台 category 组装 | 随批：capacity 口径随 roster API 统一收口；「项目广场」公开区移交展示平台模块 |
| **4 过程与档案（本轮）** | migrate_21：ProjectTemplate/Node、CampMilestone、CampSubmissionVersion（+MinIO 附件）、CampOutcome、CampArchive/Revision / 模板三起点+实例化（增删节点调时间）/ 节点级双模式提交（team=负责人交老师审；member=个人交负责人审）/ 成果核验（未核验不入档案）/ 结营冻结（close 迁移自动触发，幂等）+修正版本化 / 归档全端点只读保护补漏（含未测不可逆项验证）/ 资产回流打标 | |
| **5 课程委托** | CourseOffering 三表 / 学习进度绑实例（Q15 落地） / 权限统一 | 取代原「模板建课」 |
| **6 清理与扩展** | 旧志愿写入口移除评估 / 统计导出 / 临床调研营、比赛营、搜索、主页、感谢信模板、等级系统 | |

---

## 9. 风险与开放问题

| # | 事项 | 应对 |
|---|---|---|
| 1 | **两端 git 分叉风险**：同事会话所在机器的 flask `Icebear_develop` 也本地领先（其文档 §5.1 记录领先上游 2 提交），Mac 侧昨日又提交 gratitude 未 push | **开工前先统一**：Mac 侧 push（需授权）→ 对端 pull，或反向；否则 1a 的清账改动会撞车 |
| 2 | 状态图未冻结（H-004）+ 资格导入标识未定（H-001 尾巴） | 阶段 0 三个确认点；不阻塞 1a |
| 3 | 归档写保护现状有遗漏 | 阶段 1 修复并测试化 |
| 4 | 48 处门禁清账替换错一处即权限回归 | 恒等替换+preflight+零引用验收+冒烟矩阵（v1.0 §4.3 原文有效） |
| 5 | ~~市集页退役波及视觉分支投入~~（v1.2 解除：市集保留为志愿收集入口，视觉优化直接受益） | 阶段条/托盘按单轮化微调 |
| 6 | Q15 学习进度跨期 | 方向已定（CourseEnrollment 实例化），阶段 5 落地；此前 user_course UQ 现状维持 |
| 7 | 真库并发正确性 | 事务测试为硬门槛，mock 不得替代 |

---

## 10. 与同事文档的冲突裁决（需要用户/老师最终确认的 4 点）

| # | 冲突 | 同事文档 | 本方案裁决（默认执行） | 理由 |
|---|---|---|---|---|
| ① | 平台身份层级 | §11.4 保留四级 student/mentor/teacher/super_admin | **两级**（super_admin/user + admin_tag），同事三层身份的「营期/单元身份」照搬 | 昨天 A5 已拍板+1a 已清账；同事方案的资格名单/单元身份恰好补齐了「营内身份」的完整形态，两者是互补而非对立 |
| ② | 状态枚举 | preparing/running/closed/cancelled+双子流程（自称未冻结） | 采纳该基线，替代 v1.0 的 draft/upcoming/active/archived | 子流程窗口模型解决了「导生报名/学员报名/勾选」三窗口塞主状态的别扭；`cancelled` 补齐了缺失的取消态 |
| ③ | 大组（软件/硬件组） | 未提及 | **沿用 ms_tags 标签，不建表**；结构化需求出现再升级 | A13-1 定位是纯类别标签；现状 tags 已满足；符合「不用做得太智能」 |
| ④ | 项目跨年续做 | 未直接对应（项目状态机含 completed/terminated） | 新营期重新申报+Profile 复制，不做自动「重启」 | 简化；档案已冻结历史，「续做」本质是新批次；若老师要显式续做链路，阶段 6 加 `continued_from` 字段 |
| ⑤ | **志愿是否在平台收集** | R-013/Q-004 决策 6：「平台不收集志愿」 | **收集**（单轮 3 志愿）+ 导出 CSV + 线下协调 + 批量指派回填；仅废除两轮反选/抢选/自动匹配 | Q-004 记录的用户原话只说「简化导生选择学生的逻辑」，未说取消志愿收集；用户 2026-09-02 明确纠正「本来就计划平台上收集 123 志愿然后导出线下解决」。**需同步回同事会话修订其 R-013 红线，避免两份方案漂移** |

另：同事文档 §26 声明其本身**禁止提交**（本地文件），本方案可入库；两文档冲突以本文 §10 为准并回写同步。

---

## 附录：类型默认配置（CampPolicy 默认值，代码常量）

```python
CATEGORY_DEFAULTS = {
  'learning': {
    'label': '培训营（学习型）',
    'units': ['mentor_team'],
    'application': 'join_request',          # 学员申请入池+审批
    'formation': 'preference_export',       # 单轮志愿收集+导出+线下协调+批量回填
    'match_rule': 'single_mentor',          # 每生一导生（CampLearningMatch 强唯一）
    'project_limit': None,
    'course_policy': 'admin_managed',       # 阶段 5 生效
    'capabilities': {'attendance': True, 'leave': True, 'seat': True},    # v1.3 能力开关（营期行可覆盖）
  },
  'project': {
    'label': '项目营',
    'units': ['project'],
    'application': 'join_request',
    'formation': 'preference_export',       # 项目志愿单轮+导出+线下协调+回填
    'match_rule': 'multi_project',
    'project_limit': 3,                     # 负责人自己的计入
    'course_policy': 'unit_creator',        # 阶段 5 生效
    'capabilities': {'attendance': False, 'leave': False, 'seat': False}, # v1.3：项目营首期全关（09-12 拍板）
  },
  # research / competition：枚举预留，行为未定义（比赛型走独立赛事域，不在此扩）
}
```

---

*本文 v1.3（09-12）取代 v1.2；v1.0 的 §4（RBAC/门禁清账）与 §7.1（migrate_10）全文继续有效。架构级变更先改本文再动代码。*
